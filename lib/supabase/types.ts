/**
 * lib/supabase/types.ts
 *
 * Placeholder Database type used to type the Supabase client generics.
 *
 * Once the Prisma schema is finalised (Task 2) and the Supabase project
 * is connected, replace this with the auto-generated types by running:
 *
 *   npx supabase gen types typescript --project-id <your-project-id> \
 *     --schema public > lib/supabase/types.ts
 *
 * Until then, using `any` keeps TypeScript happy without hiding real errors.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
